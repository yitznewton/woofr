<%= form_for @bark, html: {class: 'bark-form'} do |f| %>
  <%= f.text_area :body %>
  <%= f.label :bark, :barked_at do %>
    Barked at
    <%= datetime_field_tag :raw_barked_at %>
    <%= f.hidden_field :barked_at %>
  <% end %>
  <%= f.submit 'Bark!' %>
<% end %>
